# نظام الحضور والغياب - ملاحظات تقنية

## ✅ ظهور الطالبات لجميع المعلمات

### التصميم الحالي
النظام مصمم بحيث **جميع الطالبات تظهر لجميع المعلمات** في نفس المدرسة، بغض النظر عن من أضاف الطالبة.

### التنفيذ التقني

#### 1. تحميل البيانات (app.js)
```javascript
// Load students
let studentsQuery = window.supabaseClient
  .from('students')
  .select('*')
  .eq('school_id', STATE.user.school_id); // ✅ فقط بناءً على school_id

// Teachers and principal see all students in the school
// ✅ لا يوجد فلتر بـ teacher_id
```

**النتيجة:**
- عند قيام أي معلمة بإضافة طالبة، تُحفظ مع `school_id`
- جميع المعلمات في نفس المدرسة يحملون **جميع الطالبات** تلقائيًا
- الطالبات مشتركة بين جميع المعلمات

#### 2. Row Level Security (RLS) Policies
```sql
-- Teachers can view attendance for students in their school
CREATE POLICY "Teachers can view attendance in their school"
ON public.attendance FOR SELECT
USING (
    EXISTS (
        SELECT 1 FROM public.users u
        WHERE u.id = auth.uid()
        AND u.role = 'teacher'
        AND u.school_id = attendance.school_id  -- ✅ فقط بناءً على school_id
    )
);
```

**النتيجة:**
- أي معلمة في المدرسة يمكنها رؤية حضور **جميع الطالبات**
- أي معلمة يمكنها تسجيل حضور أو غياب **أي طالبة** في المدرسة
- التحديثات تظهر فورًا لجميع المعلمات

### مثال عملي

#### السيناريو:
1. **المعلمة أروى** تضيف الطالبات:
   - ليندا
   - إيلاف
   - أريام

2. **المعلمة افنان** تضيف الطالبات:
   - ديم
   - ألماس

#### النتيجة:
✅ **المعلمة أروى** ترى في حسابها:
- ليندا
- إيلاف
- أريام
- ديم *(أضافتها افنان)*
- ألماس *(أضافتها افنان)*

✅ **المعلمة افنان** ترى في حسابها:
- ليندا *(أضافتها أروى)*
- إيلاف *(أضافتها أروى)*
- أريام *(أضافتها أروى)*
- ديم
- ألماس

✅ **جميع المعلمات** يمكنهن:
- رؤية جميع الطالبات
- تسجيل حضور وغياب أي طالبة
- رؤية التحديثات الفورية من معلمات أخرى

---

## 🔄 التزامن الفوري (Real-time Sync)

### الآلية
يتم استخدام **Supabase Realtime** لمزامنة التحديثات بين حسابات المعلمات:

```javascript
// في setupRealtimeSubscriptions()
window.supabaseClient
  .channel('attendance-changes')
  .on('postgres_changes', {
    event: '*',  // INSERT, UPDATE, DELETE
    schema: 'public',
    table: 'attendance',
    filter: `school_id=eq.${STATE.user.school_id}`
  }, (payload) => {
    // تحديث STATE.data.attendance تلقائيًا
    // إعادة رسم الواجهة
  })
  .subscribe();
```

### السلوك المتوقع
1. **المعلمة أروى** تسجل حضور "ليندا" - يوم 15 فبراير = حاضر ✅
2. **فورًا** تظهر التحديثات في:
   - كشف الحضور الجماعي لأروى
   - كشف الحضور الجماعي لافنان
   - صفحة ليندا الشخصية (لأي معلمة)
   - حساب ولي أمر ليندا (قراءة فقط)

---

## 📊 الصفحات المتوفرة

### 1. التقويم الشخصي للطالبة
**المسار:** `#/teacher/student/{id}?tab=attendance`

**الميزات:**
- تقويم شهري كامل
- إحصائيات (حضور/غياب/نسب)
- النقر على أي يوم للتسجيل: ح / غ
- التنقل بين الشهور
- حفظ تلقائي

**الصلاحيات:**
- المعلمات: قراءة + كتابة
- الأهالي: قراءة فقط

### 2. كشف الحضور الجماعي
**المسار:** `#/teacher/attendance`

**الميزات:**
- جدول شامل لجميع الطالبات
- أعمدة لكل يوم في الشهر
- إحصائيات لكل طالبة (إجمالي حضور/غياب/نسب)
- اختيار الشهر من القائمة
- النقر على اسم الطالبة للذهاب لتقويمها الشخصي

**الصلاحيات:**
- المعلمات فقط

---

## 🔐 الصلاحيات حسب الدور

| الدور | رؤية الطالبات | تسجيل الحضور | رؤية كشف جماعي | ملاحظات |
|-------|---------------|---------------|----------------|----------|
| **مديرة** | جميع الطالبات | ✅ | ✅ | صلاحيات كاملة |
| **معلمة نطق** | جميع الطالبات | ✅ | ✅ | صلاحيات كاملة |
| **معلمة تربية خاصة** | جميع الطالبات | ✅ | ✅ | صلاحيات كاملة |
| **ولي أمر** | طفلها فقط | ❌ | ❌ | قراءة فقط |

---

## 📝 قاعدة البيانات

### جدول attendance
```sql
CREATE TABLE public.attendance (
    id UUID PRIMARY KEY,
    student_id UUID REFERENCES students(id),
    date DATE NOT NULL,
    status TEXT CHECK (status IN ('present', 'absent')),
    teacher_id UUID REFERENCES users(id),
    school_id UUID NOT NULL,
    created_at TIMESTAMPTZ,
    updated_at TIMESTAMPTZ,
    UNIQUE(student_id, date)  -- ضمان سجل واحد لكل طالبة في اليوم
);
```

### الفهارس (Indexes)
- `idx_attendance_student_id` - للبحث السريع بالطالبة
- `idx_attendance_date` - للبحث السريع بالتاريخ
- `idx_attendance_school_id` - للفلترة بالمدرسة
- `idx_attendance_student_date` - للبحث المركب

---

## 🧪 سيناريوهات الاختبار

### ✅ يجب اختبار:
1. **إضافة طالبة من معلمة وظهورها للمعلمات الأخريات**
2. **تسجيل حضور من معلمة ورؤيته من معلمة أخرى**
3. **التزامن الفوري بين الحسابات**
4. **الأهالي يرون حضور أطفالهم فقط (قراءة)**
5. **التقويم الشهري - التنقل بين الأشهر**
6. **كشف الحضور الجماعي - التنقل بين الأشهر**
7. **الإحصائيات دقيقة**
8. **عدم إمكانية تسجيل حضور مكرر لنفس اليوم**

---

## 🎯 الخلاصة

### ✅ تم التأكد:
- [x] جميع الطالبات تظهر لجميع المعلمات في المدرسة
- [x] لا يوجد ربط بـ `teacher_id` في استعلامات الطالبات
- [x] RLS policies تسمح بالوصول بناءً على `school_id` فقط
- [x] التزامن الفوري معد في `setupRealtimeSubscriptions()`
- [x] الأهالي يرون بيانات أطفالهم فقط

### 📌 النتيجة النهائية:
**النظام يعمل كمنصة مشتركة** حيث:
- جميع الطالبات مركزيات على مستوى المدرسة
- أي معلمة تضيف طالبة = متاحة لجميع المعلمات
- أي معلمة تسجل حضور = يظهر لجميع المعلمات فورًا
- البيانات محمية بـ RLS حسب المدرسة (ليس حسب المعلمة)
