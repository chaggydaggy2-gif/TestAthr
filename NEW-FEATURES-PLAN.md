# 📋 خطة الميزات الجديدة - منصة أثر

## ✅ ما تم إنجازه:
1. ✅ تغيير اسم المدرسة → الابتدائية 382
2. ✅ تغيير اسم المديرة → بدرية العتيبي
3. ✅ إضافة 4 معلمات (SQL جاهز)
4. ✅ المعلمات يشاهدون جميع الطالبات
5. ✅ تغيير الجدول → نظام الحصص

---

## 🚀 الميزات الجديدة المطلوبة:

### 1️⃣ تعديل قسم "متابعة الطالبة"
- [ ] إضافة فترة المتابعة (تاريخ من/إلى)
- [ ] اختيار هدفين من الخطة الفردية
- [ ] إضافة هدف يدوي (تمهيدي/استقبالي/تعبيري/نطق)
- [ ] قائمة الوسائل المستخدمة (آيباد، بطاقات، فقاعات، إلخ)

### 2️⃣ اختبار الذاكرة السمعية
- [ ] إضافة قسم جديد "اختبار الذاكرة السمعية"
- [ ] النموذج الأول (سيتم إرساله لاحقاً)
- [ ] النموذج الثاني (سيتم إرساله لاحقاً)
- [ ] ربط الاختبار بملف الطالبة

### 3️⃣ التقرير المبدئي
- [ ] إنشاء قسم "التقرير المبدئي"
- [ ] مقدمة جاهزة ثابتة
- [ ] بيانات الطالبة تلقائياً (اسم، صف، تاريخ)
- [ ] إنشاء تقرير جديد
- [ ] تعديل التقرير
- [ ] حفظ التقرير
- [ ] عرض التقارير السابقة
- [ ] طباعة التقرير

### 4️⃣ تصحيح أخطاء التقييم
- [ ] مراجعة قسم التقييم
- [ ] تصحيح الأخطاء الإملائية والصياغية

---

## 📂 التغييرات المطلوبة:

### على مستوى المعلمة:
- صفحة الطالبة → إضافة تبويبات جديدة
- متابعة الطالبة → تحديث النموذج
- التقييم → تصحيح الأخطاء

### على مستوى الطالبة (Student Side):
- **مهم:** التأكد من أن الطالبات يمكنهم رؤية:
  - التقارير المبدئية (للقراءة فقط)
  - نتائج اختبار الذاكرة السمعية
  - متابعة الأهداف

### على مستوى ولي الأمر:
- عرض التقارير المبدئية
- عرض نتائج اختبارات الذاكرة السمعية
- عرض متابعة الأهداف

---

## 🗂️ البنية الجديدة لملف الطالبة:

```
ملف الطالبة
├── النماذج (موجود)
├── الخطة الفردية (موجود)
├── متابعة الطالبة (تعديل) ⭐
├── اختبار الذاكرة السمعية (جديد) ⭐
├── التقرير المبدئي (جديد) ⭐
├── التقييم (تصحيح) ⭐
└── رسائل ولي الأمر (موجود)
```

---

## 📊 قاعدة البيانات - الجداول الجديدة:

### 1. جدول `student_followups` (متابعة الطالبة):
```sql
- id
- student_id
- date_from
- date_to
- goal_1_id (من الخطة)
- goal_2_id (من الخطة)
- custom_goal_1 (هدف يدوي)
- custom_goal_1_type (تمهيدي/استقبالي/تعبيري/نطق)
- custom_goal_2
- custom_goal_2_type
- tools (jsonb array: آيباد، بطاقات، إلخ)
- notes
- created_at
- updated_at
```

### 2. جدول `auditory_memory_tests`:
```sql
- id
- student_id
- test_type (1 or 2)
- test_data (jsonb)
- score
- notes
- tested_by (teacher_id)
- tested_at
```

### 3. جدول `initial_reports`:
```sql
- id
- student_id
- report_date
- introduction (auto-filled)
- content (jsonb sections)
- created_by (teacher_id)
- created_at
- updated_at
```

---

## 🎯 خطة التنفيذ:

### المرحلة 1: متابعة الطالبة (يوم 1)
- تعديل النموذج
- إضافة حقول قاعدة البيانات
- ربط بالخطة الفردية

### المرحلة 2: اختبار الذاكرة السمعية (يوم 1-2)
- إنشاء الجدول
- إضافة النماذج (بانتظار البيانات)
- واجهة الاختبار

### المرحلة 3: التقرير المبدئي (يوم 2)
- إنشاء الجدول
- نموذج التقرير
- طباعة PDF

### المرحلة 4: تصحيح التقييم (يوم 2-3)
- مراجعة النصوص
- تصحيح الأخطاء

---

**سأبدأ الآن بالتنفيذ!** 🚀
