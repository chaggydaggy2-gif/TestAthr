# تحويل النماذج إلى PDF فقط - معلمات التربية الخاصة

## 📋 ما تم التغيير؟

تم تحويل **نموذجين** في صفحة معلمات التربية الخاصة إلى **رفع PDF فقط** (بدون محتويات داخلية):

### ✅ النماذج المتأثرة:

#### 1️⃣ **التقييم القبلي** (`preAssessment`)
- **قبل:** نموذج كامل مع أقسام وأسئلة (مثل معلم النطق) + خيار رفع PDF
- **بعد:** **فقط** زر رفع PDF - لا توجد محتويات داخلية

#### 2️⃣ **ملاحظة الطالبة** (`studentNotes`)
- **قبل:** notes manager مع إضافة ملاحظات متعددة
- **بعد:** **فقط** زر رفع PDF - لا يوجد notes manager

---

## 🎯 لماذا هذا التغيير؟

معلمات التربية الخاصة **لا يحتجن** نفس التقييم القبلي المفصّل الذي تستخدمه معلمة النطق.

بدلاً من ذلك:
- ✅ يرفعون ملف PDF جاهز (من نظام آخر أو مستند Word)
- ✅ أسرع وأبسط
- ✅ لا حاجة لتعبئة النموذج من البداية

---

## 🔍 التفاصيل التقنية

### **الملف المعدّل:**
`app.js` - السطر 1357-1362

### **القبل:**
```javascript
const SPECIAL_ED_FORM_TYPES = [
  { key: 'parentConsent',      name: 'موافقة ولي الأمر',     sub: 'الموافقة الرسمية على بدء الجلسات', icon: '✍️' },
  { key: 'initialData',        name: 'البيانات الأولية',     sub: 'بيانات الطالبة الأساسية (مشتركة بين المعلمات)', icon: '📝', shared: true },
  { key: 'preAssessment',      name: 'التقييم القبلي',        sub: 'تقييم المستوى قبل بدء الخطة',       icon: '📋', allowPDF: true },  // ❌ النموذج + PDF
  { key: 'studentNotes',       name: 'ملاحظة الطالبة',        sub: 'رفع ملف pdf — ملاحظات دورية عن الطالبة', icon: '📝', isPDF: true },
];
```

### **البعد:**
```javascript
const SPECIAL_ED_FORM_TYPES = [
  { key: 'parentConsent',      name: 'موافقة ولي الأمر',     sub: 'الموافقة الرسمية على بدء الجلسات', icon: '✍️' },
  { key: 'initialData',        name: 'البيانات الأولية',     sub: 'بيانات الطالبة الأساسية (مشتركة بين المعلمات)', icon: '📝', shared: true },
  { key: 'preAssessment',      name: 'التقييم القبلي',        sub: 'رفع ملف pdf — تقييم المستوى قبل بدء الخطة',       icon: '📋', isPDF: true },  // ✅ PDF فقط
  { key: 'studentNotes',       name: 'ملاحظة الطالبة',        sub: 'رفع ملف pdf — ملاحظات دورية عن الطالبة', icon: '📝', isPDF: true },
];
```

### **الفرق الرئيسي:**
- ❌ `allowPDF: true` → يعني: نموذج عادي + **خيار** رفع PDF
- ✅ `isPDF: true` → يعني: **فقط** رفع PDF (بدون نموذج داخلي)

---

## 📊 مقارنة الأنواع

| النموذج | معلم النطق | معلم التربية الخاصة |
|---------|------------|---------------------|
| **التقييم القبلي** | نموذج كامل مع أقسام + خيار PDF | **PDF فقط** |
| **ملاحظة الطالبة** | نموذج كامل مع أقسام + خيار PDF | **PDF فقط** |
| **اختبار النطق** | نموذج كامل | ❌ غير مرئي |
| **الذاكرة السمعية** | نموذج كامل | ❌ غير مرئي |
| **البيانات الأولية** | مشترك ✅ | مشترك ✅ |
| **موافقة ولي الأمر** | نموذج عادي | نموذج عادي |

---

## ✅ ما تم التأكد منه:

1. ✅ `renderFormsTab()` يتعرف على `isPDF: true`
2. ✅ يعرض زر "رفع" بدلاً من "إكمال"
3. ✅ إذا تم رفع PDF، يعرض زر "عرض" + "تحديث"
4. ✅ لا يوجد notes manager لـ `studentNotes`
5. ✅ لا توجد محتويات داخلية لـ `preAssessment`

---

## 🧪 الاختبار:

### **كيف تتأكدين:**

1. سجلي دخول كـ **معلمة تربية خاصة**
2. افتحي ملف طالبة
3. تبويب "النماذج"
4. يجب أن تري:
   - ✅ التقييم القبلي: زر "رفع" فقط (بدون نموذج)
   - ✅ ملاحظة الطالبة: زر "رفع" فقط (بدون نموذج)
   - ✅ البيانات الأولية: نموذج عادي (مشترك)
   - ✅ موافقة ولي الأمر: نموذج عادي

---

## 🚨 ملاحظة مهمة:

هذا التغيير **يؤثر فقط على**:
- ✅ معلمات التربية الخاصة (`teacher_type = 'special_education'`)
- ✅ عند عرض النماذج في تبويب "النماذج"

**لا يؤثر على:**
- ❌ معلمات النطق (يستخدمون `FORM_TYPES` وليس `SPECIAL_ED_FORM_TYPES`)
- ❌ نائبة المديرة
- ❌ المديرة

---

## 📅 التاريخ:
- **آخر تحديث:** 8 سبتمبر 2026
- **Commit:** `62983e4`

---

## 🔗 ملفات ذات صلة:
- `app.js` (السطر 1357-1362) - تعريف النماذج
- `app.js` (السطر 1670-1740) - عرض النماذج
- `SPECIAL-ED-FORMS-REMOVED.md` - النماذج المخفية سابقاً

